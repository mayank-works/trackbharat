// HeroContainer has been replaced by PageShell, which now wraps the
// entire homepage (Hero + TrackingShowcase + RailwayNetwork) as a
// single continuous shell. This file is kept only to preserve the
// module path; new code should import PageShell directly.
export { default } from "./PageShell";
